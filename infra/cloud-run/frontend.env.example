# Reference only. VITE_API_BASE_URL is baked in at Docker build time via --build-arg,
# not read at Cloud Run runtime (static Nginx serving a prebuilt SPA bundle).

VITE_API_BASE_URL=https://stock-lens-api-xxxxx.a.run.app
